import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Configuration","description":"","frontmatter":{},"headers":[],"relativePath":"guide/configuration.md","filePath":"guide/configuration.md","lastUpdated":null}');
const _sfc_main = { name: "guide/configuration.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h1><p>Most configuration is managed from the browser in Settings. Environment variables cover process-level behavior and secrets needed before the UI can load.</p><h2 id="environment-variables" tabindex="-1">Environment Variables <a class="header-anchor" href="#environment-variables" aria-label="Permalink to &quot;Environment Variables&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Variable</th><th>Required</th><th>Description</th></tr></thead><tbody><tr><td><code>JWT_SECRET</code></td><td>Yes</td><td>Session signing secret. Must be at least 32 characters.</td></tr><tr><td><code>PUID</code></td><td>No</td><td>User ID used by the container entrypoint. Defaults to <code>1000</code>; Unraid commonly uses <code>99</code>.</td></tr><tr><td><code>PGID</code></td><td>No</td><td>Group ID used by the container entrypoint. Defaults to <code>1000</code>; Unraid commonly uses <code>100</code>.</td></tr><tr><td><code>PORT</code></td><td>No</td><td>Listen port. Defaults to <code>2121</code>.</td></tr><tr><td><code>BIND_HOST</code></td><td>No</td><td>Bind address. Defaults to <code>0.0.0.0</code>.</td></tr><tr><td><code>CONFIG_DIR</code></td><td>No</td><td>Runtime data directory. Defaults to <code>./config</code> locally and <code>/app/config</code> in Docker.</td></tr><tr><td><code>PUBLIC_BASE_URL</code></td><td>Recommended</td><td>Public URL for links and emails. Include the subpath if the app is not hosted at the domain root.</td></tr><tr><td><code>BASE_PATH</code></td><td>No</td><td>URL prefix when hosted under a subpath, such as <code>/portal</code>.</td></tr><tr><td><code>FORCE_SECURE_COOKIES</code></td><td>Recommended for HTTPS</td><td>Set to <code>true</code> when the portal is served over HTTPS.</td></tr><tr><td><code>ALLOW_PRIVATE_INTEGRATION_URLS</code></td><td>No</td><td>Allows private/LAN URLs when saving integration settings.</td></tr><tr><td><code>SETUP_TOKEN</code></td><td>No</td><td>Enables remote first-time setup with an explicit token.</td></tr><tr><td><code>CLIENT_ID</code></td><td>No</td><td>Fixed Plex OAuth client id. Auto-generated if unset.</td></tr></tbody></table><p>See <code>.env.example</code> for the full template.</p><h2 id="settings-ui" tabindex="-1">Settings UI <a class="header-anchor" href="#settings-ui" aria-label="Permalink to &quot;Settings UI&quot;">​</a></h2><p>Important browser-managed settings include:</p><table tabindex="0"><thead><tr><th>Area</th><th>What You Configure</th></tr></thead><tbody><tr><td>Media Player</td><td>Plex or Jellyfin mode</td></tr><tr><td>Plex</td><td>Admin token, selected server, optional direct server URL</td></tr><tr><td>Jellyfin</td><td>Server URL, API key, login behavior, Quick Connect</td></tr><tr><td>Analytics</td><td>Tautulli for Plex or JellyStat for Jellyfin</td></tr><tr><td>Branding and UI</td><td>Logo, splash assets, accent colors, default theme</td></tr><tr><td>Access</td><td>Temporary access length, expiry rules, inactivity cleanup</td></tr><tr><td>SMTP</td><td>Host, port, username, password, sender address</td></tr><tr><td>Newsletters</td><td>Weekly or monthly schedule and content behavior</td></tr><tr><td>Home Layout</td><td>Section ordering and visibility</td></tr><tr><td>Media Stack</td><td>Sonarr and Radarr URLs and API keys</td></tr><tr><td>Status Page</td><td>Public service checks and health URLs</td></tr></tbody></table><h2 id="security-defaults" tabindex="-1">Security Defaults <a class="header-anchor" href="#security-defaults" aria-label="Permalink to &quot;Security Defaults&quot;">​</a></h2><p>The server refuses to start without a strong <code>JWT_SECRET</code>.</p><p>Session cookies are <code>httpOnly</code> and use <code>sameSite: lax</code>. Set <code>FORCE_SECURE_COOKIES=true</code> only when the app is served over HTTPS, typically behind a reverse proxy.</p><p>Admin-only API routes require an authenticated admin session. Plex admins are verified through server ownership, and Jellyfin admins are verified through Jellyfin policy data.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/configuration.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const configuration = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  configuration as default
};
